<odoo>
    <!-- trying to create an inherited form view from sale_management and to
         extend it -->
    <record model="ir.ui.view" id="view_order_form">
      <field name="inherit_id" ref="sale.view_order_form"/>
      <field name="name">sale.order.form</field>
      <field name="model">exercise.exercise</field>
      <field name="arch" type="xml">
          <!-- attempting to put the 2 additional fields after the 'note' field -->
          <xpath expr="//field[@name='note']" position="after">
              <field name="exercise"/>
              <field name="exercise_rot13"/>
          </xpath>
      </field>
    </record>




    <!-- explicit list view definition (from scaffolding template) -->
    <!--
    <record model="ir.ui.view" id="exercise.list">
      <field name="name">exercise list</field>
      <field name="model">exercise.exercise</field>
      <field name="arch" type="xml">
        <tree>
          <field name="exercise"/>
          <field name="exercise_rot13"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models (from scaffolding template) -->
    <!--
    <record model="ir.actions.act_window" id="exercise.action_window">
      <field name="name">exercise window</field>
      <field name="res_model">exercise.exercise</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="exercise.action_server">
      <field name="name">exercise server</field>
      <field name="model_id" ref="model_exercise_exercise"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="exercise" id="exercise.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Sales order with “exercise”" id="exercise.menu_1" parent="exercise.menu_root"/>
    -->
    <!-- <menuitem name="Menu 2" id="exercise.menu_2" parent="exercise.menu_root"/> -->
    <!-- actions -->
    <!--
    <menuitem name="List sales orders" id="exercise.menu_1_list" parent="exercise.menu_1"
              action="exercise.action_window"/>
    -->
    <!--
    <menuitem name="Server to list" id="exercise" parent="exercise.menu_2"
              action="exercise.action_server"/>
    -->
</odoo>
